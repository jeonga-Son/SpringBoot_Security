server:
  port: 8081

## jpa, h2
spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username : sa
    password:

## JWT
token:
  expiration_time: 86400000
  secret: user_token
